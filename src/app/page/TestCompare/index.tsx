import React, { FC } from "react";
import { View, Text, StyleSheet } from "react-native";

interface Props {}

const TestCompare: FC<Props> = () => {
    return (
        <View>
            <Text>TestCompare</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default TestCompare;
